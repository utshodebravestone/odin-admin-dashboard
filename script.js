if (window.innerWidth < 1250) {
  console.log(window.innerWidth);
  alert(
    "Your window is too small to use this app properly. You could perhaps enable your device's Landscape Mode too see and use the app properly."
  );
}
